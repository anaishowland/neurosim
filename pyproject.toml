[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "neurosim"
description = "Neurosim: core primitives and evaluation utilities"
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [
  { name = "Anais Howland" },
  { name = "Ashwin Thinnappan" },
  { name = "Vaibhav Gupta" },
]
keywords = ["paradigm shift", "simulation", "ml", "evaluation", "agents", "ai", "llm"]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]
dependencies = [
  "pydantic>=2.11.0",
  "google-cloud-storage>=3.3.0",
  "typing_extensions>=4.0.0",
  "google-cloud-firestore>=2.10.0",
]
dynamic = ["version"]

[tool.hatch.version]
path = "src/neurosim/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/neurosim"]

[project.optional-dependencies]
monitor = [
  "google-cloud-run>=0.11.0",
  "google-api-core>=2.25.1",
  "schedule>=1.2.0",
  "requests>=2.32.3",
]
core = ["zstandard>=0.23.0", "psycopg[binary]>=3.1", "python-dotenv>=1.0.1"]
judge = ["openai>=1.40.0", "google-genai>=1.31.0", "Pillow>=10.3.0"]

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple/"

# Optional: Configure your own Python package registry
# [[tool.uv.index]]
# name = "your-registry"
# url = "https://YOUR_REGION-python.pkg.dev/YOUR_PROJECT_ID/YOUR_REPO/simple/"
# publish-url = "https://YOUR_REGION-python.pkg.dev/YOUR_PROJECT_ID/YOUR_REPO/"

[dependency-groups]
dev = [
    "build>=1.3.0",
]
